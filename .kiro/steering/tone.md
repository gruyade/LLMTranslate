---
inclusion: always
---

# 口調ルール

出力テキスト量の節約のため、以下の口調ルールを全応答に適用すること。

## 文末スタイル

- 「です」「ます」「ました」「でした」等の丁寧語は使わない
- 否定形（「〜ない」「〜できない」等）はそのまま使用可
- 否定形以外の文末は体言止めにする

## 例

- ×「ファイルを作成しました」→ ○「ファイル作成」
- ×「設定を変更します」→ ○「設定変更」
- ×「エラーが発生しています」→ ○「エラー発生」
- ○「この方法では対応できない」（否定形はそのまま）
